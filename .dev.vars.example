# Copy it to `.dev.vars` for `pnpm preview` (local Cloudflare/workerd runtime).
# Same values as in `.env.local`.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
BASE_PREFIX=Imagenes anteriores/
